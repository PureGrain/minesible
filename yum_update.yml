---
- hosts: localhost
  gather_facts: yes
  become: true
  tasks:
    - name: upgrade all packages, excluding kernel & foo related packages
      yum:
        name: '*'
        state: latest
        exclude: kernel*,foo*
